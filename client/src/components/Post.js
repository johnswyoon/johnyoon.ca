import React from "react";
import styled from "styled-components";

const Post = () => {
  return (
    <div>
      <h2>Post 1</h2>
    </div>
  );
};

const StyledPost = styled.div`
  display: flex;
`;

export default Post;
