import React from "react";
import styled from "framer-motion";

// Add Functionality
// image grid template to pass pictures
// for different themes: cars, portraits, etc

const ImageGrid = () => {
  return <StyledImageGrid></StyledImageGrid>;
};

const StyledImageGrid = styled.div`
  display: grid;
`;

export default ImageGrid;
